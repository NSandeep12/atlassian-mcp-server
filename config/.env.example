# =============================================================================
# Atlassian MCP Server Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Jira Configuration
# -----------------------------------------------------------------------------
# Your Jira instance URL (without trailing slash)
JIRA_URL=https://your-domain.atlassian.net

# Your Jira email address
JIRA_EMAIL=your-email@company.com

# Jira API token (get from: https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_API_TOKEN=your-jira-api-token-here

# -----------------------------------------------------------------------------
# Bitbucket Configuration
# -----------------------------------------------------------------------------
# Your Bitbucket username
BITBUCKET_USERNAME=your-username

# Your Bitbucket workspace name
BITBUCKET_WORKSPACE=your-workspace

# Optional: Bitbucket App Password (for API operations)
# Get from: https://bitbucket.org/account/settings/app-passwords/
# BITBUCKET_APP_PASSWORD=your-app-password

# Optional: OAuth credentials (alternative to app password)
# BITBUCKET_OAUTH_KEY=your-oauth-key
# BITBUCKET_OAUTH_SECRET=your-oauth-secret

# -----------------------------------------------------------------------------
# Git Configuration
# -----------------------------------------------------------------------------
# Default branch name for new repositories
GIT_DEFAULT_BRANCH=main

# Local path where repositories will be cloned
GIT_REPOS_PATH=/app/repos

# Git user configuration (for commits)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your-email@company.com

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Server host (0.0.0.0 for Docker, localhost for local development)
MCP_SERVER_HOST=0.0.0.0

# Server port
MCP_SERVER_PORT=8000

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Maximum number of concurrent requests
MAX_CONCURRENT_REQUESTS=10

# Request timeout in seconds
REQUEST_TIMEOUT=30

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# Enable/disable SSL verification (set to false only for development)
SSL_VERIFY=true

# API rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=60

# Session timeout in minutes
SESSION_TIMEOUT=60

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
# Enable/disable specific features
ENABLE_JIRA_INTEGRATION=true
ENABLE_BITBUCKET_INTEGRATION=true
ENABLE_GIT_OPERATIONS=true
ENABLE_WEBHOOKS=false

# -----------------------------------------------------------------------------
# Development Configuration
# -----------------------------------------------------------------------------
# Enable debug mode (more verbose logging, detailed error messages)
DEBUG_MODE=false

# Enable development features
DEV_MODE=false

# Mock external services (for testing)
MOCK_SERVICES=false

# -----------------------------------------------------------------------------
# Monitoring and Observability
# -----------------------------------------------------------------------------
# Enable metrics collection
ENABLE_METRICS=true

# Metrics endpoint path
METRICS_PATH=/metrics

# Health check endpoint path
HEALTH_CHECK_PATH=/health

# Enable request tracing
ENABLE_TRACING=false

# -----------------------------------------------------------------------------
# Database Configuration (if using persistent storage)
# -----------------------------------------------------------------------------
# Database URL (optional, for caching and session storage)
# DATABASE_URL=sqlite:///app/data/mcp-server.db

# Redis URL (optional, for caching)
# REDIS_URL=redis://localhost:6379/0

# -----------------------------------------------------------------------------
# Notification Configuration
# -----------------------------------------------------------------------------
# Slack webhook URL (optional, for notifications)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Email configuration (optional, for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM_EMAIL=noreply@yourcompany.com
